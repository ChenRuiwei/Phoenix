/* GNU ld script
   Use the shared library, but some functions are only in
   the static library, so try that secondarily.  */
OUTPUT_FORMAT(elf64-littleriscv)
GROUP ( /usr/riscv64-linux-gnu/lib/libc.so.6 /usr/riscv64-linux-gnu/lib/libc_nonshared.a  AS_NEEDED ( /usr/riscv64-linux-gnu/lib/ld-linux-riscv64-lp64d.so.1 ) )
